import Route from '@ember/routing/route';

/**
 * This would contain requesting the user, authentication, etc.
 * Reserving the application route for loading resources and any other high-level
 * tasks that are not immediately related to the app.
 */
export default class extends Route {}
